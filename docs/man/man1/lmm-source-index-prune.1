.nh
.TH "LMM" "1" "Sep 2026" "lmm 2.0.0" "User Commands"

.SH NAME
lmm-source-index-prune - Remove local source indexes nothing needs


.SH SYNOPSIS
\fBlmm source index prune [flags]\fP


.SH DESCRIPTION
Remove the local search indexes (Thunderstore's) that no game needs.

.PP
An index no game in games.yaml maps is removed. An index a game does map is
kept until it has gone 30 days without a refresh. --all removes every index
regardless, after asking (-y skips the question). Every index is a copy of a
public catalogue, so anything removed is rebuilt the next time it is
searched.

.PP
Pruning is fail-closed: nothing is removed if games.yaml cannot be read,
no unused index is removed while a game maps the source to an identifier
lmm cannot use, and a directory is only removed when it holds nothing but
the index files lmm wrote - never through a symbolic link. --dry-run lists
exactly what a real run would remove, and removes nothing.

.PP
Examples:
  lmm source index prune --dry-run
  lmm source index prune
  lmm source index prune --all -y


.SH OPTIONS
\fB--all\fP[=false]
	remove every index, including ones a game uses

.PP
\fB--dry-run\fP[=false]
	list what would be removed, and remove nothing

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for prune

.PP
\fB-y\fP, \fB--yes\fP[=false]
	skip the confirmation --all asks for


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--config\fP=""
	config directory (default: $XDG_CONFIG_HOME/lmm or ~/.config/lmm)

.PP
\fB--data\fP=""
	data directory (default: $XDG_DATA_HOME/lmm or ~/.local/share/lmm)

.PP
\fB-g\fP, \fB--game\fP=""
	game ID to operate on

.PP
\fB--json\fP[=false]
	output JSON instead of text; mutating commands print their result, --dry-run prints the plan; never prompts

.PP
\fB--log-level\fP="off"
	diagnostic log level written to stderr (off, error, warn, info, debug)

.PP
\fB--no-color\fP[=false]
	disable colored output (NO_COLOR env is also honored)

.PP
\fB--no-hooks\fP[=false]
	disable all hooks

.PP
\fB-s\fP, \fB--source\fP=""
	the source whose index to use (default: every source of the game that keeps one)

.PP
\fB-v\fP, \fB--verbose\fP[=false]
	verbose output


.SH SEE ALSO
\fBlmm-source-index(1)\fP


.SH HISTORY
10-Sep-2026 Auto generated by spf13/cobra
