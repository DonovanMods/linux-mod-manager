.nh
.TH "LMM" "1" "Jul 2026" "lmm 2.0.0" "User Commands"

.SH NAME
lmm-source-add - Add a user-defined source from a definition file


.SH SYNOPSIS
\fBlmm source add <file> [flags]\fP


.SH DESCRIPTION
Validate a source definition file and install it under the config
directory's sources/ folder, where it is loaded at every start.

.PP
The definition's own id names the file it is written as. An id already
taken by a built-in source is refused (a definition reusing one would
lose the collision at startup and never take effect), as is a definition
that validates but cannot be constructed - a directory source pointing
at a path that does not exist, say.

.PP
Examples:
  lmm source add ./my-mods.yaml
  lmm source add ./my-mods.yaml --json


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for add


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--config\fP=""
	config directory (default: $XDG_CONFIG_HOME/lmm or ~/.config/lmm)

.PP
\fB--data\fP=""
	data directory (default: $XDG_DATA_HOME/lmm or ~/.local/share/lmm)

.PP
\fB-g\fP, \fB--game\fP=""
	game ID to operate on

.PP
\fB--json\fP[=false]
	output JSON instead of text; mutating commands print their result, --dry-run prints the plan; never prompts

.PP
\fB--log-level\fP="off"
	diagnostic log level written to stderr (off, error, warn, info, debug)

.PP
\fB--no-color\fP[=false]
	disable colored output (NO_COLOR env is also honored)

.PP
\fB--no-hooks\fP[=false]
	disable all hooks

.PP
\fB-v\fP, \fB--verbose\fP[=false]
	verbose output


.SH SEE ALSO
\fBlmm-source(1)\fP


.SH HISTORY
27-Jul-2026 Auto generated by spf13/cobra
