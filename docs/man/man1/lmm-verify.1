.\" Man page for lmm verify
.TH LMM-VERIFY 1 "2026-01-29" "lmm" "User Commands"
.SH NAME
lmm verify \- Verify cached mod files
.SH SYNOPSIS
.B lmm verify
[\fIMOD_ID\fR] [\fIOPTIONS\fR]
.SH DESCRIPTION
Verify the integrity of cached mod files using stored checksums. Without
arguments, verifies all cached mods for the specified game. With a mod ID,
verifies only that mod. Checks are done per mod version; missing cache is reported.
.SH OUTPUT
.TP
.B + ModName (fileID) \- OK
Cache exists for that mod version and checksum is stored.
.TP
.B X ModName (fileID) \- MISSING (version X not in cache)
Cached files for that mod version are missing. Use \fB\-\-fix\fR to re-download.
.TP
.B ? ModName (fileID) \- NO CHECKSUM
No checksum stored (e.g. installed with \fB\-\-skip-verify\fR or before checksum support).
.SH OPTIONS
.TP
.BR \-g ", " \-\-game\fR=ID
Game ID (required unless default is set).
.TP
.BR \-p ", " \-\-profile\fR=NAME
Profile to verify (default: default).
.TP
.BR \-\-fix
Re-download missing or corrupted files.
.SH SEE ALSO
.BR lmm (1),
.BR lmm-install (1).
