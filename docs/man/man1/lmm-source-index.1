.nh
.TH "LMM" "1" "Sep 2026" "lmm 2.0.0" "User Commands"

.SH NAME
lmm-source-index - Show, rebuild or list the local search indexes of sources like Thunderstore


.SH SYNOPSIS
\fBlmm source index [flags]\fP


.SH DESCRIPTION
Show or rebuild the local search index a source keeps for the active game.

.PP
Thunderstore publishes each community's whole catalogue as one document and
has no search endpoint, so lmm keeps a copy under the cache directory and
searches that. It is built on the first search, refreshed every six hours,
and costs from a few megabytes to a couple of hundred for the largest
community. --refresh rebuilds it now - the command to reach for when a
package published in the last few hours does not show up.

.PP
--all lists every index on disk, for every game, with its size and the
games that use it; 'lmm source index prune' removes the ones nothing needs.

.PP
Examples:
  lmm source index --game lethal-company
  lmm source index --refresh
  lmm source index --all
  lmm source index prune --dry-run


.SH OPTIONS
\fB--all\fP[=false]
	list every index on disk, for every game

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for index

.PP
\fB--refresh\fP[=false]
	rebuild the index now, even if it is current

.PP
\fB-s\fP, \fB--source\fP=""
	the source whose index to use (default: every source of the game that keeps one)


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--config\fP=""
	config directory (default: $XDG_CONFIG_HOME/lmm or ~/.config/lmm)

.PP
\fB--data\fP=""
	data directory (default: $XDG_DATA_HOME/lmm or ~/.local/share/lmm)

.PP
\fB-g\fP, \fB--game\fP=""
	game ID to operate on

.PP
\fB--json\fP[=false]
	output JSON instead of text; mutating commands print their result, --dry-run prints the plan; never prompts

.PP
\fB--log-level\fP="off"
	diagnostic log level written to stderr (off, error, warn, info, debug)

.PP
\fB--no-color\fP[=false]
	disable colored output (NO_COLOR env is also honored)

.PP
\fB--no-hooks\fP[=false]
	disable all hooks

.PP
\fB-v\fP, \fB--verbose\fP[=false]
	verbose output


.SH SEE ALSO
\fBlmm-source(1)\fP, \fBlmm-source-index-prune(1)\fP


.SH HISTORY
10-Sep-2026 Auto generated by spf13/cobra
